<j:jelly xmlns:j="jelly:core" xmlns:st="jelly:stapler" xmlns:d="jelly:define" xmlns:l="/lib/layout" xmlns:t="/lib/hudson" xmlns:f="/lib/form">
  <f:entry title="${%Spring BeanDoc directory}"
    description="${%description}">
    <f:textbox name="beandoc_dir" value="${instance.beandocDir}"
        checkUrl="'${rootURL}/publisher/SpringBeanDocArchiver/check?job=${it.fullName}&amp;value='+escape(this.value)"/>
  </f:entry>
</j:jelly>