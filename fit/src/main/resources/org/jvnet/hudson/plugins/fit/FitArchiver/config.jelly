<j:jelly xmlns:j="jelly:core" xmlns:st="jelly:stapler" xmlns:d="jelly:define" xmlns:l="/lib/layout" xmlns:t="/lib/hudson" xmlns:f="/lib/form">
  <f:entry title="${%Path to HTML Report}"
    help="${rootURL}/plugin/fit/help-projectConfig.html"
    description="${%description}">
    <f:textbox name="fit.pathToHtml" value="${instance.pathToHtml}"
        checkUrl="'${rootURL}/publisher/FitArchiver/check?job=${it.fullName}&amp;value='+escape(this.value)"/>
  </f:entry>
</j:jelly>
